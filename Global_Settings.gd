extends Node

#static variables so we can tune for balance
static var pup_speed_dur = 1.0
#power up: speed, duration
static var pup_hook_dur = 1.0
static var pup_cannon_dur = 1.0

var master_vol = 1.0
var sfx_vol = 0.6
var music_vol = 0.2




func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
